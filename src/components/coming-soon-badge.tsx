import Row from "@/src/components/core/row";

const ComingSoonBadge = () => {
  return (
    <Row className="rounded-full bg-neutral-900 p-2 h-2 w-fit items-center shrink-0">
      <span className="text-neutral-50 text-xs">Em breve</span>
    </Row>
  );
};

export default ComingSoonBadge;
